import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.chalkboard.physics',
  appName: 'chalkboard-physics',
  webDir: 'out'
};

export default config;
